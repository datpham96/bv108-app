import React from 'react';
import {SafeAreaView, View, Text} from 'react-native';

const Library = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Library</Text>
      </View>
    </SafeAreaView>
  );
};

export default Library;

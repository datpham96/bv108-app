import React from 'react';
import {SafeAreaView, View, Text} from 'react-native';

const Account = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Account</Text>
      </View>
    </SafeAreaView>
  );
};

export default Account;
